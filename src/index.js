import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import "./index.css"; // optional if using Tailwind or CSS

ReactDOM.createRoot(document.getElementById("root")).render(<App />);
